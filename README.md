# april-quickstart

personal quickstart for various kinds of code projects

- different templates included in `templates/`
- install a template with `npm run cli init <template> <dest>`
  - e.g. `npm run cli init node ~/repos/my-project`
